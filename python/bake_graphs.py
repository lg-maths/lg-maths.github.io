if __name__ == "__main__":
	...
	# iterating in `user-content/graphs`
	# using `importlib`, import the `doit` methods inside the python files
	# run the methods, get the Figure, save the figure in `website/src/assets/svg-data/`